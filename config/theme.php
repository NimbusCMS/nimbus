<?php

declare(strict_types=1);

/**
 * The active public theme.
 *
 * Return the name of a directory under themes/. Nimbus renders public pages
 * with that theme's plain-PHP templates. This mirrors config/plugins.php:
 * configuration lives in one place, in one form.
 *
 * The bundled 'starter' theme is the default; copy it to themes/your-theme/
 * and point this at it to make the site your own.
 *
 *   return 'your-theme';
 */

return 'starter';
